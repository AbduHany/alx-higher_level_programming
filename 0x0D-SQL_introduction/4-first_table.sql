-- This script creats a table called first_table in the current database.
-- This query creates a table called first_table with id and name attrs.
CREATE TABLE IF NOT EXISTS first_table (
id INT,
`name` VARCHAR(256)
);
